{
  "name": "ts-lerna-boilerplate",
  "description": "",
  "version": "1.0.0",
  "author": "Faraz Syed <hello@farazsyed.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "setup": "rm -rf git && git init && rm package.json && cp internals/* . && rm -rf internals && npm install"
  }
}